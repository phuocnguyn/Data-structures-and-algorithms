#include <iostream>
#include <math.h>
using namespace std;
bool KiemTraSoNguyenTo(int n)
{   
    if (n <= 1 && n % 2 == 0) return false;
    for (int i = 2; i < sqrt(n); i++)
    {
        
    }
    
}