#include <iostream>

using namespace std;

int count_digits(int number) {
  number = abs (number);
  if (number == 0) return 1;
  else 
    
}

int main() {
  int number;
  cin >> number;
  //Các bạn hãy hoàn thành đoạn mã tại đây
}